import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.komplett',
  appName: 'Komplett',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
